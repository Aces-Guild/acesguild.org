{
  "name": "acesguild-org",
  "type": "module",
  "version": "0.0.1",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync": "node scripts/sync-projects.js",
    "sync:local": "node scripts/sync-projects.js --local"
  },
  "dependencies": {
    "astro": "^4.0.0"
  }
}
